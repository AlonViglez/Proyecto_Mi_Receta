pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //link de la grafica
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "nav-drawer"
include ':app'
